import 'dart:typed_data';

Future<Float32List> decodeAudioToPcm(Uint8List bytes, int targetSampleRate) async {
  throw UnsupportedError('Dekode audio tidak didukung di platform ini secara bawaan. Silakan gunakan format WAV.');
}
